# Model and experiment configurations
# ------------------------------------
# This file contains configurations for model parameters,
# data paths, and experiment settings.

# Example configuration
model:
  name: "VisionTransformer"
  learning_rate: 0.001
  epochs: 100

data:
  path: "datasets/"
  batch_size: 32
